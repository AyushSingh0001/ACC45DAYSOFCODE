# ACC45DAYSOFCODE
#include <iostream>
using namespace std;

int main() 
{
        int  t,l,r,c;
        cin >> t;
        while(t--)
        {
                cin >> l >> r;
                c=0;
              while(l<=r)
              {
                      if( ((l%10)==(2)) || ((l%10)==(3))  || ((l%10)==(9)) )
                      {
                              c++;
                      }
                              l++;
              }
        cout << c << endl;
        }
	return 0;
}
